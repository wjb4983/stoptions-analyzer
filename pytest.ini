[pytest]
testpaths =
    tests
    tests/ui
markers =
    smoke: fast smoke coverage that should run on every PR.
    core: core correctness and governance checks that should run on every PR.
    slow: long-running regression checks for main/nightly pipelines.
